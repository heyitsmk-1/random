# Copy to .env and fill in. Never commit .env.
PIXELLAB_API_KEY=      # pixel-art generation (api.pixellab.ai)
ELEVENLABS_API_KEY=    # voice rendering for the scripted lines
OPENROUTER_API_KEY=    # LLM, for the live path (see days-off/README.md)
DATABASE_URL=          # postgres
